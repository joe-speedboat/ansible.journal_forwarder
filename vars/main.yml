---
# Packages by OS family
pkg_fluent_bit:
  - fluent-bit

pkg_rhel_fluent_bit:
  - fluent-bit
  - systemd-libs

pkg_debian_fluent_bit:
  - fluent-bit

pkg_rhel_journal_forwarder_repositories:
  - epel-release

pkg_rhel_journal_forwarder_audit:
  - audit

pkg_debian_journal_forwarder_audit:
  - auditd

pkg_debian_repository_dependencies:
  - ca-certificates

svc_fluent_bit: fluent-bit
svc_audit:
  RedHat: auditd
  Debian: auditd

fluent_bit_rpm_repo_family_map:
  Rocky: rockylinux
  AlmaLinux: almalinux
  RedHat: rockylinux

...
