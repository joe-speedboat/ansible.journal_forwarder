#!/usr/bin/ansible-playbook
---
- name: Test journal_forwarder role
  hosts: localhost
  connection: local
  vars:
    graylog_gelf_host: "127.0.0.1"
  roles:
    - joe-speedboat.journal_forwarder
...
